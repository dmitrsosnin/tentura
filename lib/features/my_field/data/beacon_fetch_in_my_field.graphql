# import '/data/gql/beacon/_fragments.graphql'

query BeaconFetchInMyField($ego: String!) {
  scores(ego: $ego, where: {score: {gt: 0}, node: {like: "B"}}) {
    beacon {
      ...BeaconFields
    }
  }
}
