import 'package:flutter/material.dart';

class MyReactionsPage extends StatelessWidget {
  const MyReactionsPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Nothing here yet'));
  }
}
