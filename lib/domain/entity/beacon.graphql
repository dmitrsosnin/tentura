# import 'user.graphql'

fragment BeaconFields on beacon {
  id
  title
  description
  created_at
  updated_at
  is_pinned
  enabled
  lat
  long
  timerange
  has_picture
  comments_count
  my_vote
  author {
    ...UserFields
  }
}
