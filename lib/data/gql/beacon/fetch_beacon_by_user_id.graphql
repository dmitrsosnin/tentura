# import '_fragments.graphql'

query FetchBeaconsByUserId($user_id: String!) {
  beacon(where: {user_id: {_eq: $user_id}}, order_by: {created_at: desc}) {
    ...beaconFields
    comments_aggregate {
      aggregate {
        count
      }
    }
  }
}
